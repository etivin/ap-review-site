# raw extracted PDF images (optimized versions already committed as .jpg)
images/frq-*.png

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# local tooling and drafts (kept on disk, out of the repo)
.claude/
mockups/
SECURITY-NOTES.md

# AP scoring guideline PDFs (reference material, not site content)
ap*-sg-world-history-modern-*.pdf
